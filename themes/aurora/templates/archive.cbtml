extends base
slot content
  div.archive
    h1 {{ year }}年{{ month }}月
    div.post-list
      for post in posts
        article.post-card
          h2
            a [href="{{ post.url }}"] {{ post.title }}
          time {{ post.created_at }}
      end
