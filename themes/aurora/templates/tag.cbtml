extends base
slot content
  div.tag-archive
    h1 标签：{{ tag }}
    div.post-list
      for post in posts
        article.post-card
          h2
            a [href="{{ post.url }}"] {{ post.title }}
          time {{ post.created_at }}
      end
