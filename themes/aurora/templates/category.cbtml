extends base
slot content
  div.category-archive
    h1 分类：{{ category }}
    div.post-list
      for post in posts
        article.post-card
          h2
            a [href="{{ post.url }}"] {{ post.title }}
          time {{ post.created_at }}
      end
