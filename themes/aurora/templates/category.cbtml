extends base
slot content
  div.category-archive
    h1.archive-title 分类：{{ category }}
    div.post-list
      for post in posts
        article.archive-item
          time.archive-date {{ post.created_at | date }}
          h2.archive-post-title
            a [href="{{ post.url }}"] {{ post.title }}
      end
